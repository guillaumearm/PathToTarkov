import type { ILocationBase } from '@spt/models/eft/common/ILocationBase';

export type MyType = ILocationBase;

describe('this is a dumb test', () => {
  it('should work', () => {
    expect(true).toBe(true);
  });
});
